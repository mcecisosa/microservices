const response = require("./response");
const catchedAsync = require("./catchedAsync");
const errors = require("./errors");

module.exports = {
  response: response,
  catchedAsync: catchedAsync,
  errors: errors,
};
